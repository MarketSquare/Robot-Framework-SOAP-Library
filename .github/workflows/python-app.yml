name: SOAP-Library

on:
  push:
    branches:
      - master
      - version1.0
  pull_request:
    branches:
      - master

jobs:
  run-robot-tests-py3-10:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.10
        uses: actions/setup-python@v2
        env:
          ROBOT_TESTS_DIR: ${{ github.workspace }}/Tests
          ROBOT_REPORTS_DIR: ${{ github.workspace }}/reports
        with:
          python-version: '3.10'
      - name: Install Dependencies
        run: pip install -r requirements.txt
      - name: Run Robot File
        run: robot -d ./reports ./Tests/keyword_tests.robot
      - name: Upload test results
        uses: actions/upload-artifact@v1
        if: always()
        with:
          name: robot_reports
          path: reports

  run-robot-tests-py3-9:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        env:
          ROBOT_TESTS_DIR: ${{ github.workspace }}/Tests
          ROBOT_REPORTS_DIR: ${{ github.workspace }}/reports
        with:
          python-version: '3.9'
      - name: Install Dependencies
        run: pip install -r requirements.txt
      - name: Run Robot File
        run: robot -d ./reports ./Tests/keyword_tests.robot
      - name: Upload test results
        uses: actions/upload-artifact@v1
        if: always()
        with:
          name: robot_reports
          path: reports

  run-robot-tests-py3-8:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.8
        uses: actions/setup-python@v2
        env:
          ROBOT_TESTS_DIR: ${{ github.workspace }}/Tests
          ROBOT_REPORTS_DIR: ${{ github.workspace }}/reports
        with:
          python-version: '3.8'
      - name: Install Dependencies
        run: pip install -r requirements.txt
      - name: Run Robot File
        run: robot -d ./reports ./Tests/keyword_tests.robot
      - name: Upload test results
        uses: actions/upload-artifact@v1
        if: always()
        with:
          name: robot_reports
          path: reports
